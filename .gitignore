.venv/
venv/
__pycache__/
*.pyc
.DS_Store
.claude/

# Replayed feature tables: large and regenerated by `python -m src.train --no-cache`.
artifacts/ds_*.parquet

# Everything else that reproducibility needs is committed on purpose:
#   cache/                 archived Open-Meteo responses (~20 MB parquet) -> offline replay
#   artifacts/*.joblib     the trained model (~2.4 MB) -> forecasts without retraining

# Local exploratory analysis data copied from the scratch session (duplicates dataset/); not a deliverable.
data/

# API keys live here, never in git
.env
